User Profile App

This is a simple web application that allows users to view and update their profile.
The app uses Node.js and Express for the backend, and stores data in MongoDB.
Mongo Express is used for easy management of the database.

The app is designed to run in a Docker environment, with MongoDB and Mongo Express running in containers connected via a Docker network
